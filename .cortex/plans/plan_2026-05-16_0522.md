---
created: 2026-05-16T05:22:47.344Z
session: 005c0137-4f72-4939-be5d-84e36856b621
working_directory: /Users/abhishektiwari/Documents/dev/fraud-intelligence-platform
---

## Context

Explored the full platform: React 18 + Tailwind frontend, FastAPI backend, Kafka streaming, PyIceberg/Nessie/MinIO data layer, Ollama + ChromaDB AI stack, XGBoost/RF/IF ensemble ML service. Current state: REST endpoints return random mock data, WebSocket bridge to Kafka already works (with simulation fallback), AI copilot RAG pipeline exists in `ai-copilot/` but backend uses a simple direct-Ollama call, ML service is real but backend ML router returns fake data.

---

## Implementation Plan (12 Enhancements, Ordered by Dependencies)

### Phase 1: Backend Data Layer (enables everything else)

**Step 1: Connect REST endpoints to Iceberg tables via PyIceberg+DuckDB**
- `backend/app/routers/transactions.py` — Replace `_generate_transaction()` with real Iceberg query using existing `sql_editor.py` pattern (PyIceberg → Arrow → DuckDB). Query `bronze.raw_transactions`. Keep mock fallback if catalog unavailable.
- `backend/app/routers/fraud_alerts.py` — Same pattern, query from Kafka consumer's in-memory buffer + Iceberg persistence. Keep mock fallback.
- `backend/app/routers/ml_predictions.py` — Call the real ML service at `http://ml-service:8000/model/info` and `/metrics` instead of returning random data.

**Step 2: Ensure WebSocket→Kafka bridge is working (already functional)**
- The `kafka_consumer.py` already bridges Kafka→WebSocket with simulation fallback. No changes needed — just verify it's working.

**Step 3: Wire RAG copilot into the investigation endpoint**
- `backend/app/routers/investigation.py` — Replace the simple Ollama call with imports from `ai-copilot/chains/investigation_chain.py`. Use the RAG pipeline: retrieve context from ChromaDB → augment prompt → generate. Add streaming SSE support.
- Add `ai-copilot/` to the Python path in the backend container.
- Add a background indexer that indexes recent alerts into ChromaDB on startup.

### Phase 2: Frontend New Features

**Step 4: SQL Editor improvements — CodeMirror, query history, auto-complete, export**
- Add `@codemirror/lang-sql` + `@codemirror/view` to `package.json`
- Replace `<textarea>` in `SqlEditor.tsx` with CodeMirror editor (SQL language, dark theme, auto-completion with table/column names)
- Add query history panel (stored in localStorage, last 20 queries)
- Add "Download CSV" and "Download JSON" buttons on results

**Step 5: Alert Case Management**
- New component `frontend/src/components/AlertDetail.tsx` — Full alert detail view with:
  - Assign to analyst (dropdown)
  - Add investigation notes (textarea)
  - Mark as true positive / false positive / escalate
  - Timeline of status changes
- New route `/alerts/:id` in `App.tsx`
- Backend: Persist alert status changes to Redis (fast key-value) since there's no dedicated DB

**Step 6: Data Lineage Visualization**
- New component `frontend/src/components/DataLineage.tsx` — Shows bronze→silver→gold pipeline as a directed graph (using SVG/canvas, no heavy library)
- Shows table names, row counts (from Nessie metadata), transformation descriptions
- New nav item "Lineage" with `GitBranch` icon
- Backend: `GET /api/lineage` endpoint that returns the static DAG structure + live row counts from Iceberg table snapshots

**Step 7: Model A/B Comparison Dashboard**
- Enhance `ModelPerformance.tsx` — Add:
  - Model selector tabs (XGBoost / Random Forest / Isolation Forest / Ensemble)
  - Side-by-side metrics comparison table
  - ROC curve overlay chart (one line per model)
  - "Active model" badge showing which is deployed
- Backend: New `GET /api/ml/models` endpoint that proxies to ML service's `/model/info` and returns all registered model versions from `ml/models/registry.json`

**Step 8: Notification bell with badge**
- Modify `Layout.tsx` — Add a notification bell icon in the sidebar header showing unresolved critical alert count
- Add dropdown showing last 5 critical alerts when clicked
- Wire to WebSocket message count (already tracked in `useFraudAlerts`)

### Phase 3: Polish & Integration

**Step 9: SQL execution working end-to-end**
- Install `pyiceberg[s3]` and `duckdb` in the running container (rebuild backend image)
- Verify `POST /api/sql/execute` works with real Iceberg table data

**Step 10: Saved queries in SQL Editor**
- Add "Save Query" button → localStorage with name/description
- "Saved Queries" collapsible section in left sidebar
- Click to load a saved query into the editor

**Step 11: Export from SQL results**
- CSV export: convert columns + rows to CSV blob, trigger download
- JSON export: same as rows array of objects

**Step 12: Verification pass**
- TypeScript build (`tsc --noEmit`)
- Vite production build
- Backend syntax check
- Docker restart and verify all endpoints return real data

---

## Files Modified/Created Summary

### Backend (modify)
- `backend/app/routers/transactions.py` — Real Iceberg queries
- `backend/app/routers/fraud_alerts.py` — Real data + case management
- `backend/app/routers/ml_predictions.py` — Proxy to real ML service
- `backend/app/routers/investigation.py` — RAG pipeline integration
- `backend/app/main.py` — Register new routers (lineage)

### Backend (new)
- `backend/app/routers/lineage.py` — Data lineage API
- `backend/app/services/iceberg_reader.py` — Shared Iceberg query helper

### Frontend (modify)
- `frontend/src/components/Layout.tsx` — Notification bell, new nav items
- `frontend/src/components/SqlEditor.tsx` — CodeMirror, history, export, saved queries
- `frontend/src/components/ModelPerformance.tsx` — A/B comparison
- `frontend/src/App.tsx` — New routes
- `frontend/src/services/api.ts` — New API functions
- `frontend/src/types/index.ts` — New types
- `frontend/package.json` — Add codemirror deps

### Frontend (new)
- `frontend/src/components/AlertDetail.tsx` — Case management
- `frontend/src/components/DataLineage.tsx` — Lineage visualization
- `frontend/src/components/NotificationBell.tsx` — Alert notifications

### Infrastructure (modify)
- `infrastructure/docker/backend/Dockerfile` — Already updated with pyiceberg/duckdb

---

## Verification

1. `cd frontend && npx tsc --noEmit` — Zero type errors
2. `cd frontend && npx vite build` — Clean production build
3. `python3 -c "import ast; ast.parse(open('backend/app/routers/...').read())"` for each modified Python file
4. `docker restart fraud-backend` and test endpoints:
   - `curl localhost:8000/api/sql/tables` → real tables
   - `curl localhost:8000/api/transactions` → real Iceberg data (or mock fallback)
   - `curl localhost:8000/api/ml/metrics` → real model metrics
   - `curl localhost:8000/api/lineage` → DAG structure
5. Frontend manual check: all 9 nav items render, SQL editor executes queries, lineage graph displays, model comparison works

---

## Critical Files

- `backend/app/routers/transactions.py` — Core mock→real data conversion
- `frontend/src/components/SqlEditor.tsx` — Major enhancement (CodeMirror + history + export)
- `backend/app/routers/investigation.py` — RAG integration point
- `frontend/src/components/Layout.tsx` — Central navigation (new tabs + notification bell)
- `frontend/src/components/DataLineage.tsx` — New visualization component